version: '3.0'

services:
  api:
    build: .
    ports:
      - '${DOCKER_PORT:-8080}:${PORT:-8080}'
    restart: always
    links:
      - 'cache'
    volumes:
      - ./data:/app/data

  cache:
    image: redis
    restart: always
    volumes:
      - ./data/cache:/data
